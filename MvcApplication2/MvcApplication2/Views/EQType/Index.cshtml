@model IEnumerable<MvcApplication2.Models.EQType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EQTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EQTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EQTypeID })
        </td>
    </tr>
}

</table>
